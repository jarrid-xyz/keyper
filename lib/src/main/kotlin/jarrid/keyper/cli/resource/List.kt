package jarrid.keyper.cli.resource

import jarrid.keyper.cli.ResourceSubcommand

class List(help: String = "List existing resources") : ResourceSubcommand(help = help) {
    override suspend fun runAsync() {
        
    }
}